package skyfire.shopplugin.utils;

public class StringUtil {

    public static String replacePlaceholder(String str, String placeholder, String replacement) {
        return str.replace(placeholder, replacement);
    }
}
